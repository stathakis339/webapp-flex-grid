users.forEach(val=>{                    //Reading users from  data.js
    console.log(val.name, val.surname); // browser's console
    document.write(val.name+"<br>");    // show us name and change a line
})